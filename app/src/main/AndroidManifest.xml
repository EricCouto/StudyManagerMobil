<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.studymanagermobil">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".view.register.RegisterUser"></activity>
        <activity android:name=".view.roll.RollSubject" />
        <activity android:name=".view.roll.RollTask" />
        <activity android:name=".view.reciclyView.Task" />
        <activity android:name=".view.register.RegisterSubject" />
        <activity android:name=".view.register.RegisterTask" />
        <activity android:name=".view.reciclyView.Subject" />
        <activity android:name=".view.master.Home">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>